* {
  box-sizing: border-box;
}
global-reset()
ol{
  list-style: inherit
}
select, button, input, textarea {
  font-size: inherit;
}
button{
  padding: 0;
  border-width: 0;
  outline: none;
  background-color: transparent;
  color: inherit;
}
